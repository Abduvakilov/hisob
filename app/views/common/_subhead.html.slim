ul.nav.nav-tabs.mb-3
  - ['show', 'new_report'].each do |action|
    - if lookup_context.exists?(action, controller_name) || \
      lookup_context.exists?(action, 'application')
      li.nav-item
        = link_to t(action, scope: 'views.tab_titles'), {action: action, id: controller.object}, class: "nav-link#{' active' if action_name==action}"
