<!DOCTYPE html>
<html lang="uz">
<head>
	<title><%= title = yield(:title).presence || title_of_page %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
	<%= csp_meta_tag %>

	<%= javascript_pack_tag 'application', defer: true %>
	<%= stylesheet_pack_tag 'application' %>
	<% if head = yield(:head).presence -%>
		<%= head %>
	<%end-%>
</head>

<body>

	<%= render 'common/menu' %>

	<div class="main-content">

		<%= render 'common/flash' %>

		<div class="container mb-3">

			<h1><%= title %></h1>		

			<% if content = yield(:custom_yield).presence %>
				<%= content %>
			<% else %>
				<div class="row">

					<div class="col-md-<%=col=@col||8%>">
						<%= yield %>		
					</div>

					<% if aside = yield(:aside).presence -%>
					<aside class="col-md-<%=12-col%> mt-2 mt-md-0">
						<%= aside %>		
					</aside>
					<%end-%>

				</div>
			<%end-%>

		</div>

	</div>

</body>
</html>
