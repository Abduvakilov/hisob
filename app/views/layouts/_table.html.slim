table.table.table-hover.table-light.table-responsive
	thead
		tr
			- records.model.shown_fields.each do |column|
				== th_sortable Transaction, column
	tbody
		- records.each do |t|
			tr data-url=polymorphic_path(t)
				- records.model.shown_fields.each do |column|
					- if respond_to? "format_#{column}"
						td == public_send("format_#{column}", t)
					- else
						td = t.send(column)
== paginate records